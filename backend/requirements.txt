# SEAI Backend Dependencies
# Install with: pip install -r requirements.txt

# Sentence Transformers — for semantic similarity (embedder.py)
sentence-transformers>=2.7

# HuggingFace Transformers — for Qwen2.5-Coder-7B-Instruct (generator.py)
transformers>=4.45

# Accelerate — required for device_map="auto" in generator.py
accelerate>=0.26

# PyTorch — required by both models above
torch>=2.0

# NumPy — for vector operations in embedder.py and loader.py
numpy>=1.26

